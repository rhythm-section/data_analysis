<h1>Listing user_activities</h1>

<table>
  <thead>
    <tr>
      <th>Activity timestamp</th>
      <th>Activity name</th>
      <th>Activity type</th>
      <th>Activity confidence</th>
      <th>Device</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user_activities.each do |user_activity| %>
      <tr>
        <td><%= user_activity.activity_timestamp %></td>
        <td><%= user_activity.activity_name %></td>
        <td><%= user_activity.activity_type %></td>
        <td><%= user_activity.activity_confidence %></td>
        <td><%= user_activity.device_id %></td>
        <td><%= link_to 'Show', user_activity %></td>
        <td><%= link_to 'Edit', edit_user_activity_path(user_activity) %></td>
        <td><%= link_to 'Destroy', user_activity, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User activity', new_user_activity_path %>
